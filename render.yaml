services:
  - type: web
    name: occupantkiller
    runtime: node
    buildCommand: "echo 'No build required — static server'"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /healthz
    numInstances: 1
    plan: free
